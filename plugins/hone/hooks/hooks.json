{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"${CLAUDE_PLUGIN_ROOT}/hooks/pre-deploy-gate.sh\"; if [ -f \"$f\" ]; then bash \"$f\"; fi"
          }
        ]
      }
    ]
  }
}
